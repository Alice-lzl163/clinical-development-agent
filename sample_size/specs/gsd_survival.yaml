{
  "test_key": "gsd_survival",
  "method_id": "gs_survival",
  "display_name": "Group-sequential log-rank",
  "endpoint_family": "time_to_event",
  "design_family": "group_sequential",
  "estimand": "Hazard-ratio effect under a prespecified group-sequential survival design.",
  "hypothesis": {
    "null": "HR = thetaH0 at each look",
    "alternative": "HR = planned alternative",
    "effect_measure": "hazard_ratio",
    "beneficial_direction": "lower_or_higher_explicit"
  },
  "inputs": [
    {
      "name": "alternative_hazard_ratio",
      "definition": "Hazard ratio under the design alternative.",
      "type": "number",
      "unit": "dimensionless",
      "required": true,
      "valid_range": {
        "exclusive_minimum": 0
      },
      "default": null,
      "clinical_interpretation": "Hazard ratio under the design alternative."
    },
    {
      "name": "control_median",
      "definition": "Control median survival used by the accrual/event model.",
      "type": "number",
      "unit": "time",
      "required": true,
      "valid_range": {
        "exclusive_minimum": 0
      },
      "default": null,
      "clinical_interpretation": "Control median survival used by the accrual/event model."
    },
    {
      "name": "accrual_duration",
      "definition": "Enrollment duration.",
      "type": "number",
      "unit": "time",
      "required": true,
      "valid_range": {
        "exclusive_minimum": 0
      },
      "default": null,
      "clinical_interpretation": "Enrollment duration."
    },
    {
      "name": "follow_up_duration",
      "definition": "Additional follow-up after accrual.",
      "type": "number",
      "unit": "time",
      "required": true,
      "valid_range": {
        "exclusive_minimum": 0
      },
      "default": null,
      "clinical_interpretation": "Additional follow-up after accrual."
    },
    {
      "name": "dropout_probability",
      "definition": "Cumulative independent dropout probability over the stated horizon.",
      "type": "number",
      "unit": "dimensionless",
      "required": true,
      "valid_range": {
        "minimum": 0,
        "maximum": 1
      },
      "default": null,
      "clinical_interpretation": "Cumulative independent dropout probability over the stated horizon."
    },
    {
      "name": "allocation_ratio",
      "definition": "Planned treatment-to-control sample-size ratio n_treatment / n_control.",
      "type": "number",
      "unit": "dimensionless",
      "required": true,
      "valid_range": {
        "exclusive_minimum": 0
      },
      "default": 1,
      "clinical_interpretation": "Planned treatment-to-control sample-size ratio n_treatment / n_control."
    },
    {
      "name": "alpha",
      "definition": "Overall type-I error probability.",
      "type": "number",
      "unit": "dimensionless",
      "required": true,
      "valid_range": {
        "exclusive_minimum": 0,
        "exclusive_maximum": 1
      },
      "default": 0.05,
      "clinical_interpretation": "Maximum prespecified probability of a false positive across the tested hypothesis."
    },
    {
      "name": "power",
      "definition": "Target probability of rejecting the null under the design alternative.",
      "type": "number",
      "unit": "dimensionless",
      "required": true,
      "valid_range": {
        "exclusive_minimum": 0,
        "exclusive_maximum": 1
      },
      "default": 0.8,
      "clinical_interpretation": "Target probability of rejecting the null under the design alternative."
    },
    {
      "name": "design_specification",
      "definition": "Prespecified rpact sequential design.",
      "type": "object",
      "unit": "design",
      "required": true,
      "valid_range": null,
      "default": null,
      "clinical_interpretation": "Prespecified rpact sequential design."
    }
  ],
  "alpha": {
    "sidedness": "one_or_two_sided",
    "convention": "Overall alpha; one-sided uses alpha and two-sided divides alpha equally between tails."
  },
  "allocation": {
    "ratio_definition": "n_treatment / n_control",
    "supported": true,
    "rounding_rule": "Return integer arm sizes satisfying allocation; round upward and re-evaluate achieved power."
  },
  "solve_modes": {
    "sample_size": true,
    "power": true,
    "events": true,
    "operating_characteristics": false
  },
  "engine": {
    "engine_family": "rpact",
    "package": "rpact",
    "package_version_policy": "Pin exact validated version in the project lockfile; any version change triggers documented revalidation.",
    "function": "rpact::getSampleSizeSurvival",
    "parameter_mapping": {
      "design_specification": "design",
      "alternative_hazard_ratio": "theta/lambda/median mapping",
      "allocation_ratio": "allocationRatioPlanned",
      "accrual/dropout inputs": "documented survival arguments"
    },
    "output_mapping": {
      "events": "maxNumberOfEvents",
      "sample_size": "maxNumberOfSubjects"
    },
    "runtime": "R",
    "formal_arguments": [
      {
        "name": "design",
        "required": false,
        "package_default": "NULL",
        "semantic_role": "Package formal populated from one or more declared user inputs or derived quantities in parameter_mapping."
      },
      {
        "name": "groups",
        "required": false,
        "package_default": "2",
        "semantic_role": "Package-level control/default; the adapter may use it only as declared by the frozen method specification."
      },
      {
        "name": "thetaH0",
        "required": false,
        "package_default": "1",
        "semantic_role": "Package-level control/default; the adapter may use it only as declared by the frozen method specification."
      },
      {
        "name": "pi1",
        "required": false,
        "package_default": "0.5",
        "semantic_role": "Package-level control/default; the adapter may use it only as declared by the frozen method specification."
      },
      {
        "name": "pi2",
        "required": false,
        "package_default": "0.2",
        "semantic_role": "Package-level control/default; the adapter may use it only as declared by the frozen method specification."
      },
      {
        "name": "lambda1",
        "required": false,
        "package_default": "NA_real_",
        "semantic_role": "Package-level control/default; the adapter may use it only as declared by the frozen method specification."
      },
      {
        "name": "lambda2",
        "required": false,
        "package_default": "NA_real_",
        "semantic_role": "Package-level control/default; the adapter may use it only as declared by the frozen method specification."
      },
      {
        "name": "median1",
        "required": false,
        "package_default": "NA_real_",
        "semantic_role": "Package-level control/default; the adapter may use it only as declared by the frozen method specification."
      },
      {
        "name": "median2",
        "required": false,
        "package_default": "NA_real_",
        "semantic_role": "Package-level control/default; the adapter may use it only as declared by the frozen method specification."
      },
      {
        "name": "eventTime",
        "required": false,
        "package_default": "12",
        "semantic_role": "Package-level control/default; the adapter may use it only as declared by the frozen method specification."
      },
      {
        "name": "accrualTime",
        "required": false,
        "package_default": "0",
        "semantic_role": "Package-level control/default; the adapter may use it only as declared by the frozen method specification."
      },
      {
        "name": "accrualIntensity",
        "required": false,
        "package_default": "0",
        "semantic_role": "Package-level control/default; the adapter may use it only as declared by the frozen method specification."
      },
      {
        "name": "dropoutRate1",
        "required": false,
        "package_default": "0",
        "semantic_role": "Package-level control/default; the adapter may use it only as declared by the frozen method specification."
      },
      {
        "name": "dropoutRate2",
        "required": false,
        "package_default": "0",
        "semantic_role": "Package-level control/default; the adapter may use it only as declared by the frozen method specification."
      },
      {
        "name": "dropoutTime",
        "required": false,
        "package_default": "12",
        "semantic_role": "Package-level control/default; the adapter may use it only as declared by the frozen method specification."
      },
      {
        "name": "allocationRatioPlanned",
        "required": false,
        "package_default": "1",
        "semantic_role": "Package formal populated from one or more declared user inputs or derived quantities in parameter_mapping."
      },
      {
        "name": "directionUpper",
        "required": false,
        "package_default": "TRUE",
        "semantic_role": "Package-level control/default; the adapter may use it only as declared by the frozen method specification."
      },
      {
        "name": "maxNumberOfSubjects",
        "required": false,
        "package_default": "NA_real_",
        "semantic_role": "Package-level control/default; the adapter may use it only as declared by the frozen method specification."
      }
    ]
  },
  "method_type": "numerical",
  "dropout_handling": "Inflate the analysis-required total using ceil(N / (1 - dropout_rate)); round randomized arm totals afterward.",
  "rounding_rule": "Return integer arm sizes satisfying allocation; round upward and re-evaluate achieved power.",
  "assumptions": [
    "The time_to_event endpoint model and group_sequential design are correctly specified.",
    "All required design-identifying inputs are fixed prospectively and use mutually consistent units, orientation, and analysis populations.",
    "The planned primary analysis matches the hypothesis and engine mapping in this specification."
  ],
  "unsupported_domains": [],
  "warnings": [],
  "validation_grade": "V1",
  "migration_action": "KEEP",
  "lifecycle_status": "VALIDATION_PENDING",
  "primary_reference": "Jennison & Turnbull; Wassmer & Brannath",
  "package_reference": "https://docs.rpact.org/reference/index.html",
  "guideline_reference": "FDA Adaptive Guidance; ICH E9",
  "benchmark_requirements": [
    "rpact vignette reproduction; simulated crossing probabilities, T1E, power, expected n/events",
    "Reproduce official package examples for the pinned version.",
    "Compare forward power and inverse sample-size results on a prespecified grid.",
    "Verify null-boundary type-I error and clinically relevant alternatives independently."
  ]
}
