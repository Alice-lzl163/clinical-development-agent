{
  "test_key": "cluster",
  "method_id": "cluster_design_effect_equal",
  "display_name": "Cluster-randomized design effect",
  "endpoint_family": "generic",
  "design_family": "cluster_randomized",
  "estimand": "Inflated sample size under an equal-size exchangeable-ICC design effect.",
  "hypothesis": {
    "null": "Inherited from underlying endpoint method",
    "alternative": "Inherited from underlying endpoint method",
    "effect_measure": "design_effect",
    "beneficial_direction": "not_applicable"
  },
  "inputs": [
    {
      "name": "individual_level_sample_size",
      "definition": "Validated analyzable sample size from the underlying individually randomized method.",
      "type": "number",
      "unit": "subjects",
      "required": true,
      "valid_range": {
        "exclusive_minimum": 0
      },
      "default": null,
      "clinical_interpretation": "Validated analyzable sample size from the underlying individually randomized method."
    },
    {
      "name": "mean_cluster_size",
      "definition": "Expected equal cluster size.",
      "type": "number",
      "unit": "subjects/cluster",
      "required": true,
      "valid_range": {
        "exclusive_minimum": 0
      },
      "default": null,
      "clinical_interpretation": "Expected equal cluster size."
    },
    {
      "name": "intraclass_correlation",
      "definition": "Exchangeable within-cluster correlation.",
      "type": "number",
      "unit": "dimensionless",
      "required": true,
      "valid_range": {
        "minimum": 0,
        "maximum": 1
      },
      "default": null,
      "clinical_interpretation": "Exchangeable within-cluster correlation."
    },
    {
      "name": "cluster_size_cv",
      "definition": "Coefficient of variation of cluster sizes; must be zero for this approximation.",
      "type": "number",
      "unit": "dimensionless",
      "required": true,
      "valid_range": {
        "minimum": 0
      },
      "default": 0,
      "clinical_interpretation": "Coefficient of variation of cluster sizes; must be zero for this approximation."
    },
    {
      "name": "dropout_rate",
      "definition": "Expected proportion without an analyzable primary endpoint.",
      "type": "number",
      "unit": "dimensionless",
      "required": true,
      "valid_range": {
        "minimum": 0,
        "exclusive_maximum": 1
      },
      "default": 0,
      "clinical_interpretation": "Anticipated loss applied only after the analysis-required sample size is obtained."
    }
  ],
  "alpha": {
    "sidedness": "one_or_two_sided",
    "convention": "Overall alpha; one-sided uses alpha and two-sided divides alpha equally between tails."
  },
  "allocation": {
    "ratio_definition": "Not applicable.",
    "supported": false,
    "rounding_rule": "Not applicable."
  },
  "solve_modes": {
    "sample_size": true,
    "power": false,
    "events": false,
    "operating_characteristics": false
  },
  "engine": {
    "engine_family": "analytical",
    "package": null,
    "package_version_policy": "Not applicable; pin the specification and implementation commit.",
    "function": null,
    "parameter_mapping": {},
    "output_mapping": {},
    "runtime": "NONE",
    "formal_arguments": []
  },
  "method_type": "analytical_asymptotic",
  "dropout_handling": "Inflate the analysis-required total using ceil(N / (1 - dropout_rate)); round randomized arm totals afterward.",
  "rounding_rule": "Return integer arm sizes satisfying allocation; round upward and re-evaluate achieved power.",
  "assumptions": [
    "The generic endpoint model and cluster_randomized design are correctly specified.",
    "All required design-identifying inputs are fixed prospectively and use mutually consistent units, orientation, and analysis populations.",
    "The planned primary analysis matches the hypothesis and engine mapping in this specification."
  ],
  "unsupported_domains": [
    "Unequal cluster sizes, small number of clusters, stratified/pair-matched CRTs, cluster dropout, or unspecified underlying endpoint method."
  ],
  "warnings": [
    "Approximation only; requires cluster_size_cv = 0 and a separately validated individual-level sample size."
  ],
  "validation_grade": "V3",
  "migration_action": "KEEP_AND_VALIDATE",
  "lifecycle_status": "VALIDATION_PENDING",
  "primary_reference": "Donner & Klar; Hayes & Moulton",
  "package_reference": null,
  "guideline_reference": "ICH E9",
  "benchmark_requirements": [
    "Closed-form identity tests; simulate mixed/GEE analysis; compare specialized CRT software; vary CV of cluster size",
    "Reproduce official package examples for the pinned version.",
    "Compare forward power and inverse sample-size results on a prespecified grid.",
    "Verify null-boundary type-I error and clinically relevant alternatives independently."
  ]
}
